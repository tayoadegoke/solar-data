import React from 'react'

interface Props { }

function LocationDetails(props: Props) {
    const { } = props

    return (
        <div>
            hello from details
        </div>
    )
}

export default LocationDetails
